# MCP Server Base Module - Outputs
#
# Outputs:
# - service_discovery_dns (Cloud Map DNS name)
# - service_url (Full URL to Streamable HTTP endpoint)
# - alb_url (ALB-routed URL path)
# - security_group_id (Security group ID)
# - service_name (ECS service name)
# - target_group_arn (ALB target group ARN)
# - task_role_name (ECS task IAM role name)
# - task_role_arn (ECS task IAM role ARN)
#
# Enterprise component. Full configuration available under license.
# https://www.outcomeops.ai
